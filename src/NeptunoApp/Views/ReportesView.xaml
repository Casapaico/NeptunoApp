<UserControl x:Class="NeptunoApp.Views.ReportesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Reporte: Detalle de pedidos por rango de fechas"
                       FontSize="18" FontWeight="Bold"/>
            <TextBlock Foreground="Gray" TextWrapping="Wrap"
                       Text="Ejecuta usp_DetallesPedidos_PorRangoFechas (INNER JOIN DetallesPedidos - Pedidos). Los datos se cargan en un DataTable en modo DESCONECTADO."/>
        </StackPanel>

        <Border Grid.Row="1" Background="#F3F4F6" Padding="10" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Desde:" VerticalAlignment="Center"/>
                <DatePicker SelectedDate="{Binding FechaInicio}" Width="140" Margin="6,0,16,0"/>
                <TextBlock Text="Hasta:" VerticalAlignment="Center"/>
                <DatePicker SelectedDate="{Binding FechaFin}" Width="140" Margin="6,0,16,0"/>
                <Button Content="Generar reporte" Width="130" Height="30" Command="{Binding GenerarCommand}"/>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Resultado}" AutoGenerateColumns="True"
                  IsReadOnly="True" CanUserAddRows="False"/>

        <TextBlock Grid.Row="3" Text="{Binding Mensaje}" FontWeight="Bold"
                   Foreground="#166534" Margin="0,10,0,0"/>
    </Grid>
</UserControl>
