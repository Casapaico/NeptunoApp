<UserControl x:Class="NeptunoApp.Views.CategoriasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="320"/>
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Column="0" Margin="0,0,12,0">
            <TextBlock DockPanel.Dock="Top" Text="Mantenimiento de Categorias"
                       FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <DataGrid ItemsSource="{Binding Lista}" SelectedItem="{Binding Seleccionada}"
                      AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id"          Binding="{Binding IdCategoria}"     Width="50"/>
                    <DataGridTextColumn Header="Nombre"      Binding="{Binding NombreCategoria}" Width="180"/>
                    <DataGridTextColumn Header="Descripcion" Binding="{Binding Descripcion}"     Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>

        <StackPanel Grid.Column="1">
            <TextBlock Text="Datos" FontWeight="Bold" Margin="0,0,0,8"/>

            <TextBlock Text="Id"/>
            <TextBox Text="{Binding IdCategoria}" IsReadOnly="True" Margin="0,2,0,10" Padding="5"/>

            <TextBlock Text="Nombre *"/>
            <TextBox Text="{Binding NombreCategoria, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,10" Padding="5"/>

            <TextBlock Text="Descripcion"/>
            <TextBox Text="{Binding Descripcion, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True" Height="80" TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" Margin="0,2,0,12" Padding="5"/>

            <UniformGrid Columns="2">
                <Button Content="Nuevo"    Height="34" Margin="0,0,4,4" Command="{Binding NuevoCommand}"/>
                <Button Content="Guardar"  Height="34" Margin="4,0,0,4" Command="{Binding GuardarCommand}"/>
                <Button Content="Eliminar" Height="34" Margin="0,4,4,0" Command="{Binding EliminarCommand}"/>
                <Button Content="Refrescar" Height="34" Margin="4,4,0,0" Command="{Binding RefrescarCommand}"/>
            </UniformGrid>

            <TextBlock Text="{Binding Mensaje}" Foreground="#B45309" TextWrapping="Wrap" Margin="0,12,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
